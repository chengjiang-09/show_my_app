import { PayloadAction } from '@reduxjs/toolkit'
import { ProjectDetil } from './index'


export const projectDetilReducer = (state: ProjectDetil, action: PayloadAction<string>) => {

    return state
}